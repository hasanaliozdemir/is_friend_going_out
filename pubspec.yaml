name: is_friend_going_out
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3

  cached_network_image: ^2.2.0+1

  firebase_auth: ^0.14.0+5
  cloud_firestore: ^0.12.9+4

  provider: ^3.1.0

  flutter_spinkit: "^4.0.0"

  google_fonts: ^0.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/gidioz.png
